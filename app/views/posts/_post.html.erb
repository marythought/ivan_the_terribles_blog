<section class="post">

  <h2>Post <%= "#{post.id}: #{post.title}" %></h2>
  <div class="post"><%= post.body %></div>
  <%= render 'post_nav', post: post %>
  <%= render @comments %>
  <p>See more comments: </p><%= will_paginate @comments %>

</section>
